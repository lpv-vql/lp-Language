# Lp言語
この言語はScratchのブロック操作をテキストベースに抽象化したDSLです。

## 言語仕様
#### コード内の日本語が表すもの
数→num型、文字列→str型、リスト→list型、条件→bool型、何か→特に決まってない
### 制御
#### wait
`wait 数;` の形で〇秒待つことができます。

また、`wait 条件;` の形で条件がtrueになるまで待つことができます。
```
wait 5;
wait a > 10;
```
#### repeat
